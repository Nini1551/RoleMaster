export interface User {
  username: string;
  email: string;
  password: string;
}

export interface Character {
  id: string;
  name: string;
}

export interface CharacterNote {
  id: string;
  name: string;
  note: string;
}